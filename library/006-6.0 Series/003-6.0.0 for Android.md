# 6.0.0 for Android

Note: SQLite(distributed with Qt) are built in the backage and have -DFEATURE_sql_sqlite=ON -DFEATURE_system_sqlite=OFF.

Note: <del>All packages have -DINPUT_openssl=linked, Since BoringSSL, which is used in Android 6.0 or later, is NOT API COMPATIBLE WITH OPENSSL, we link it to a static OpenSSL 1.1.1 series instead of system SSL library.</del>  
Linked OpenSSL support is broken in 6.0.0. For OpenSSL support in 6.0.0 we temporarily use runtime OpenSSL support.

Note: All builds have -DFEATURE_icu=OFF because I think the ICU library is too big and, in most of the conditions, useless for common users.

Note: All builds are using __NDK r21d__ with `-DANDROID_NATIVE_API_LEVEL=24`, it means the APK built using these packages may be not able to run on Android 7.0.x and earlier.

## Dynamic Builds

### Windows Host

Build host: Windows 10 20H2(Build 10.0.19042)

Note: Built using 64-bit NDK and 64-bit MinGW, make sure you are using 64-bit windows and have mingw32-make.exe in PATH.

<del>Qt6.0.0-Android-ALL-Clang-NDKr21d-XWindows-x86_64-MinGW8.1.0-2020xxxx.7z [SF]() [Github]()</del> (static MinGW version compile failure)

### Linux Host

Build host: CentOS 8.3, with "yum upgrade" run before building

<del>Qt6.0.0-Android-ALL-Clang-NDKr21d-xLinux-x86_64-gcc8.3.1-2020xxxx.tar.xz [SF]() [Github]()</del> (under construction)

### macOS Host

Build host: macOS 11.0.1

<del>Qt6.0.0-Android-ALL-Clang-NDKr21d-xmacOS-x86_64-AppleClang11.0.3-2020xxxx.tar.xz [SF]() [Github]()</del> (under construction)
